Implement the Code City ANALYZER in this repo. Contract: docs/CONTRACT-repo-json.md and src/types.ts (READ BOTH FIRST — do not change them). Gate: npx vitest run tests/analyzer.test.ts — tests are FIXED, editing anything under tests/ is forbidden and will be reverted.
Fill src/analyzer/ (index.ts exports analyzeRepo(repoPath: string): Promise<RepoGraph> — replace the NotImplemented stub; add scanner.ts / parser.ts / git.ts per DESIGN.md) and make bin/analyze.ts work: node --experimental-strip-types bin/analyze.ts <repo-path> <out.json>.
Requirements: file inventory via git ls-files when the target is a git repo (fallback: fs walk skipping node_modules and .git); language by extension (only TypeScript needs full support in V1); LOC per file; imports via web-tree-sitter with the TypeScript grammar from the tree-sitter-wasms package (resolve relative import specifiers to repo-relative file paths for the imports[] edges); classes/functions as contained nodes where cheap, else empty contains[]; complexity = a simple branch-counting metric over the AST (if/for/while/case/&&/||/ternary), min 1; git metrics via plain git log (age = days from file first commit to HEAD commit date, churn = commits touching the file in the 90 days before HEAD commit date, contributors = unique authors) — never use wall-clock now. Output must pass validateRepoGraph. ESM, erasable TS only (runs under node --experimental-strip-types). Commit your work with trailer "Co-Authored-By: Codex (gpt-5.6-sol) <codex@openai.com>". Do not push.
